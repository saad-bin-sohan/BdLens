# Render Blueprint for BdLens Backend
services:
  - type: web
    name: BdLens-backend
    env: python
    region: oregon
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: BdLens-db
          property: connectionString
      - key: GEMINI_API_KEY
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: ALLOWED_ORIGINS
        value: https://BdLens.vercel.app,http://localhost:3000
      - key: ENVIRONMENT
        value: production

databases:
  - name: BdLens-db
    plan: free
    databaseName: BdLens
    user: BdLens
    postgresMajorVersion: 14
    # Note: You'll need to manually run CREATE EXTENSION vector; after database creation
